"""Your workspace. As you finish each lab, copy your working functions into
``labs/my/labNN.py`` (e.g. ``labs/my/lab01.py`` with ``bilinear_sample`` and
``msda_naive_student``). Later labs will then build on *your* code instead of
the shipped reference solutions. These files are gitignored — they're yours.
"""
